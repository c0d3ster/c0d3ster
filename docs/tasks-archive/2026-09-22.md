# Archived tasks — 2026-09-22

## #2 [stack: invoicing] Add "features" as advanced request options (enum), collapsed by default in the request form. Per docs/INVOICE_BILLING_EPIC.md Phase 1: also expand the ProjectFeature enum with the new values listed there and create featurePricing.ts mapping each feature to { label, defaultPrice, description }.

- Status: done
- PR: #77 (`feat(project-request): expand ProjectFeature enum + advanced options section`), merged 2026-09-21, branch `overnight/2026-08-06/02-project-feature-advanced-options`
- Notes: Expanded the `ProjectFeature` enum, added `src/libs/featurePricing.ts` (admin-only reference data, not yet imported anywhere), and a nullable `features` json column on `project_requests` (migration `0020_add_project_request_features.sql`, confirmed applied via `db:migrate`). `ProjectRequestForm` gained a collapsed "Advanced Options" checklist; `createProjectRequest`/`approveProjectRequest` persist and prefer the request's explicit features over project-type defaults. Post-merge, the feature taxonomy was further revised on the same PR: `ProjectRequirements` was retired entirely in favor of `ProjectFeature` as the single billable taxonomy (derived per-`ProjectType`, fully editable); dropped `ThirdPartyIntegrations`/`ProjectManagement`, renamed `Testing`→`QaLaunchTesting` and `Consultation`→`TechnicalAdvisory`, added `CustomDesign`/`EcommercePlatformIntegration`/`ContentCreation`/`MaintenanceRetainer`. See `src/graphql/schema/project.ts` and `src/libs/featurePricing.ts` for the current enum.
