# Archived tasks — 2026-09-19

## #1 [stack: solo] Add file upload for existing projects.

- Status: done
- PR: #76 (`feat: add file upload for existing projects`), merged
- Notes: Extended the existing project-logo presigned-upload pattern to arbitrary allowed file types for existing projects. Added nullable `caption`/`placement` columns to `project_files`, new `requestProjectFileUpload`/`finalizeProjectFileUpload` mutations, and a new `app/(auth)/dashboard/projects/[id]` page with upload form and file gallery. Migration `0018_add_project_file_caption_placement.sql` applied to dev DB (not prod). Full test suite passing (842 tests), types and lint clean.
